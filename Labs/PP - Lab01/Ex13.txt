#include <stdio.h>

int main()
{
    float km, ms;
    printf("Digite uma velocidade em ms: ");
    scanf("%f",&ms);
    km = ms * 3.6;
    printf("A velocidade em kmh eh %f", km);
    return 0;
}